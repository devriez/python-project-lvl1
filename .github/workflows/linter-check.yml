name: linter-check

on: push

jobs:

  linter-check:
    #runs-on: ubuntu-latest

    #steps:
     # - name: Checkout code
       # uses: actions/checkout@v2

      - name: Run Linter
        run: |
          make lint
